# Flix Frontend

Minimal Vite + React frontend for the myFlix project.

Quick start

1. Install dependencies:

```bash
cd frontend
npm install
```

2. Run dev server:

```bash
npm run dev
```

3. Set API base (optional): create a `.env` file with `VITE_API_BASE=http://localhost:8080` or your deployed API URL.
